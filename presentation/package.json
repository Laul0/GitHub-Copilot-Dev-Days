{
  "name": "github-copilot-devdays-slidev",
  "private": true,
  "scripts": {
    "dev": "slidev --no-open",
    "build": "slidev build",
    "export": "slidev export",
    "postinstall": "mkdir -p public && ln -sfn ../../assets public/assets"
  },
  "dependencies": {
    "@slidev/cli": "^0.49.0",
    "@slidev/theme-default": "latest"
  },
  "devDependencies": {
    "playwright-chromium": "^1.58.2"
  }
}
